framework:
    secret: '%env(APP_SECRET)%'

    session: true

    default_locale: en

    translator:
        enabled: true
        fallbacks: ['en']
when@test:
    framework:
        test: true
        session:
            storage_factory_id: session.storage.factory.mock_file
