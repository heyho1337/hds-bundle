
                <button type="button"
                    class="btn btn-sm btn-outline-primary mt-2"
                    data-action="click->frontend--modal#openModal"
                    data-url="{{ path('add_lang') }}"
                    data-frontend--modal-url-value="{{ path('add_lang') }}"
                >
                  {{ 'Add language'|trans }}
                </button>